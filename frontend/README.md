# Frontend

React client for FeatureWind.

## Setup
```bash
cd frontend
npm install
npm run dev
```

The app expects the API at `http://localhost:5050/api` by default (see `src/services/api.js` to adjust).
